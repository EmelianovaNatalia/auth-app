# React Authentication App

Two-factor authentication with React Query error handling.

## Installation

```bash
npm install
npm run dev
```

Open http://localhost:5173


# 🔐 React Authentication App with 2FA

Современное React приложение для авторизации с двухфакторной аутентификацией (2FA) и полной обработкой ошибок API через React Query.

## 🚀 Быстрый старт

\`\`\`bash
# Установите зависимости
npm install

# Запустите в режиме разработки
npm run dev
\`\`\`

**Откройте [http://localhost:5173](http://localhost:5173) в браузере.**

## ✨ Особенности

### 🔐 Двухшаговая авторизация
1. **Шаг 1**: Ввод email и пароля
2. **Шаг 2**: 6-значный код из Google Authenticator

### 🛡️ Обработка ошибок API
- Валидация полей на клиенте
- Все HTTP ошибки (400, 401, 403, 404, 422, 429, 500, 502, 503, 504)
- Сетевые ошибки
- Детализированные сообщения об ошибках

### 🎨 Дизайн
- Современный, минималистичный интерфейс
- Адаптивный дизайн для всех устройств
- Синий фирменный стиль
- Шрифт ABeeZee

### ⚡ Технологии
- **React 18** - Библиотека для UI
- **React Query (@tanstack/react-query)** - Управление состоянием и кэширование
- **Vite** - Быстрый сборщик

## 📁 Структура проекта

\`\`\`
src/
├── App.jsx              # Главный компонент с логикой авторизации
├── App.css              # Стили приложения
└── main.jsx            # Точка входа
\`\`\`

## 🧪 Тестовые данные

### Шаг 1 - Логин
- Любой email (требуется @)
- Пароль (минимум 6 символов)
- **Для теста ошибки**: \`wrong@example.com\` - вернет 401

### Шаг 2 - 2FA
- Введите 6 цифр
- **Для теста ошибки**: \`111111\` - неверный код, \`222222\` - слишком много попыток

## 🎯 Основные компоненты

### \`LoginStep1\`
Компонент первого шага с полями:
- Email (с иконкой)
- Password (с иконкой)
- Кнопка "Log in"

### \`LoginStep2\`
Компонент второго шага (2FA):
- 6 цифровых полей с автофокусом
- Подсказка про Google Authenticator
- Кнопка "Continue"
- Кнопка "Назад" для возврата

## ⚡ React Query

Используется для управления состоянием запросов, автоматического ретрая ошибок и кэширования данных.

## 🚀 Скрипты

\`\`\`bash
npm run dev      # Запуск в режиме разработки (localhost:5173)
npm run build    # Сборка для продакшена
npm run preview  # Предпросмотр сборки
\`\`\`
